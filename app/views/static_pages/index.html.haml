%h1 Paintr

#selected_color.box

#color_form
  = text_field_tag 'color', nil, :autofocus => true
  = button_tag 'Add Color', :id => 'add_color'

#colors

#image_form
  = text_field_tag 'image', nil, :autofocus => true
  = button_tag 'Add Image', :id => 'add_image'

#images

#canvas
  - 12000.times do
    .paint_box

= button_tag 'Save', :id => 'save'

%br
=link_to "Saved Images", images_url

